package com.example.googlepreperation.strings

import java.util.*
import kotlin.collections.HashMap
import kotlin.collections.HashSet

class SubStringPallindrome {

    private fun findPalindromes(hashSet: HashSet<String>) {
        /* while (hashSet.)
        for (i in input.length - 1..0) {
            if (input[i] == input[input.length - 1 - i])
                println(input)
        }
    }*/
    }

/*
    fun findAllSubStrings(input: String) {
        val hashSet :HashSet<String> = emptySet<>()
        val tokenizer = StringTokenizer(input)
        while (tokenizer.hasMoreTokens()) {
            val token = tokenizer.nextToken()
            if (token.length > 1) {
                hashSet.add(token)
            }
        }
        if (hashSet.isNotEmpty()){
            findPalindromes()
        }
    }
*/
}

fun main() {
    val obj = SubStringPallindrome()
 //   obj.findAllSubStrings("aba abab baba 121 12121")
}